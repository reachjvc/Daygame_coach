{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["@playwright/mcp@latest", "--no-headless"],
      "env": {
        "PLAYWRIGHT_MCP_BROWSER": "chromium"
      }
    }
  }
}
